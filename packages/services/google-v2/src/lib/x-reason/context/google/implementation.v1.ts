import { solver, programmer, aiTransition, evaluate } from './prompts';
import { getFunctionCatalog } from './functionCatalog';
import { getMetaData } from './metadata';

export {
  solver as googleSolver,
  programmer as googleProgrammer,
  aiTransition as googleAiTrasition,
  evaluate as googleEvaluate,
  getFunctionCatalog as googleFunctionCatalog,
  getMetaData as metadata,
};
