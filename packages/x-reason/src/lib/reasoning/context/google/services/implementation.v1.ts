import { solver, programmer, aiTransition, evaluate } from "./prompts";
import { getFunctionCatalog } from "./functionCatalog";
import { getMetaData } from "./metadata";

export {
    solver as googleServicesSolver,
    programmer as googleServicesProgrammer,
    aiTransition as googleServicesAiTransition,
    evaluate as googleServicesEvaluate,
    getFunctionCatalog as googleServicesFunctionCatalog,
    getMetaData as googleServicesMetadata,
}